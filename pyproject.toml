[project]
name = "audio-dome-lite"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "pyloudnorm>=0.2.0",
    "pyyaml>=6.0.3",
    "soundfile>=0.14.0",
]
